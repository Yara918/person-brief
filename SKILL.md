---
name: person-brief
description: 会前人物情报：对会面对象（人物）做公开信息检索与整理，产出一份商务 HTML 人物简报（身份定位、履历、双声道、谈资、社媒）。当用户给出人名+公司+职位，需要会前准备、BD/销售拜访、商务谈判、初次会面时使用。触发词：会前情报、人物情报、对接人、拜访对象、person research、background、meeting prep。
license: MIT
compatibility: 标准 Agent Skill 格式，跨平台通用（WorkBuddy 已实测通过；Codex 按标准格式可用，待实测；Claude Code 接 DeepSeek 模型时聊天交付不可靠，建议接官方 Claude 或强模型）。模型联网自动检索（WebSearch），无联网工具时降级为用户贴回信源。脚本需 Python 3.10+。输出自包含 HTML。
metadata:
  author: <你的名字/ID>
  version: "1.0.0"
---

# person-brief · 会前人物情报

## Workflow

1. 收集必填输入（缺一不检索，一次性列全要什么）
2. 锁定研究对象（人名+公司+职位 → 降级链）
3. **模型联网检索**（WebSearch 逐组查），直接出报告，不用用户自己搜；环境无联网工具时降级为「出清单让用户贴回」
4. 登记信源（含真实 URL）
5. 填内容字段（按字数上限，查不到写「未检索到」）
6. 渲染 HTML → **运行 scripts/verify-links.py 机器质检**（链接真实性 / 敏感词 / 来源 / HTML 结构，任一不过 = FAIL）
7. → **在聊天中贴出校验脚本的原始输出**（[OK] 或 [FAIL] 清单逐行粘贴，禁止转述、禁止自称"已通过"）
8. 交付：只有脚本输出 [OK] 才交付，交付句 `📄 报告已生成：<路径>（机器校验通过）`；[FAIL] 禁止交付，必须按清单修正后重跑

## 输入（缺一不检索）

```
人名：
公司名：
职位/角色：
（可选：会面目的）
```

- **必填三项**：人名 / 公司名 / 职位。缺一不检索，停下一次性列全要什么，等齐才开工。
- 会面目的可选（用于把谈资往会面方向引）。

**缺信息提示**（一次性列全，含降级链说明）：
```
请补齐以下三项即可开工：
人名：
公司名：
职位/角色：

说明：若公开信息查不到该人，将按降级链锁定研究对象：
① 该人（人名+公司+职位）→ ② 该公司法定代表人
→ ③ 董事长 → ④ CEO/总经理 → ⑤ C-level 高管 → ⑥ 部门负责人
全链查不到 → 如实写「未检索到可研究的人物公开信息」。
只研究公开职务身份与公开言论，不调查、不背调。
```

## Phase 1: 锁定研究对象（降级链）

**目的**：确认"研究的是哪个人"。公开职位信息与实际存在出入时，按以下顺序锁定，取第一个真实查到的：

```
① 人名 + 公司 + 职位 → 查公开信息（履历/言论/社媒）
   ├─ 查到，且公开信息能对应上他给的职位 → 就是这个人 → 研究他
   ├─ 查不到该人 → 降级 ↓
② 该公司法定代表人（工商/官网可查）
③ 董事长 → ④ CEO/总经理 → ⑤ C-level 高管 → ⑥ 部门负责人
⑦ 全链查不到 → 如实写「未检索到可研究的人物公开信息」，报告如实薄
```

**降级执行规则（硬性，禁止自作主张）**：

1. **严格按序降级，禁止跳级**：②→③→④→⑤→⑥ 逐级尝试，未试过法定代表人和董事长之前，不得直接跳到高管/部门负责人。
2. **同级内优先选与指定职位相关者**：如指定「采购总监」查无，降级到高管层时，优先选分管采购/供应链的高管；若该人公开信息太少，再按原顺序继续。
3. **降级必须明示告知**：每次降级，都要在聊天中明确说「指定人查无，已降级研究该公司<层级> <姓名/职务>」，并在报告「定位层级」标注实际命中层级（如「② 法定代表人（指定人查无，降级）」）。
4. **禁止换人**：降级目标必须是该公司真实存在的法定代表人/董事长/CEO/高管/部门负责人；**检索到名字相近的人 ≠ 降级目标**，不得擅自更换研究对象；相似人名须先询问用户确认。

- **不调查、不背调**：只研究公开职务身份 + 公开言论，不深挖隐私（住址/电话/家庭/非公开账号）、不查工商涉诉记录、不突破登录墙。
- 人物身份确认：靠公开言论/资料与用户给的信息（公司+职位）能否对应来判断；对不上 → 降级或如实说明。

## 分层查询规则（写进检索清单，用户可见）

**社媒（人物号）**：
- 必查：微博 / 领英 / X / 抖音
- 按行业查：小红书 / B站 / 知乎 / 脉脉（看人物行业与公开度）
- 每平台独立查询，命中写【账号名 + 粉丝数 + 美观链接按钮】，查不到写「未检索到 + 检索记录」
- **链接必须是账号主页**：社媒区按钮只允许指向该平台的账号主页（如 weibo.com/u/xxx、douyin.com/user/xxx、linkedin.com/in/xxx）。
- **报道 ≠ 账号**：只查到新闻报道提及账号存在、但主页未检索到时，写「未检索到账号主页，仅见报道提及」并把报道链接放检索记录，**禁止把报道链接当社媒主页按钮**。
- **操作列格式（硬性）**：社媒表第 3 列（操作）只放「进入主页」按钮或留空/「—」；**禁止在操作列写长句**（如「未检索到账号主页链接，仅见报道提及（链接）」）、**禁止在操作列放 srclink**——「未检索到/仅见报道提及」写在账号信息列（第 2 列），报道链接一律进附录检索记录。

**履历与言论**：
- 履历：公开资料/官网团队页/新闻提及（带来源）
- 言论：有公开采访/演讲/表态 → 双声道；没有 → 如实写「未检索到公开言论」（不硬凑）

## 内容字段（字数上限，查不到写「未检索到」）

| 字段 | 上限 |
|---|---|
| 身份一句话（谁/在哪/什么角色） | ≤ 40 字 |
| 背景轨迹（3-4 个节点） | 各 ≤ 40 字，带来源 |
| 双声道（💬他说 vs 📰外界） | 各 ≤ 50 字，带来源；无公开言论 → 如实空 |
| Gap 解读（自我认知差 = 谈话突破口） | ≤ 60 字 |
| 谈资（3 个） | 各 ≤ 60 字，带来源 |
| 避雷（1 个） | ≤ 60 字 |
| 检索记录（search_log） | 平台×关键词×结果，每条 ≤ 40 字 |
| 社媒账号（每平台一行） | 账号名 + 粉丝数 + 链接 |

- 查不到 = 如实写「未检索到」，绝不编造、绝不推测填充。

## 质量铁律

1. **真实采集**：每条信息有真实 URL，**来自本次联网检索结果**；禁止生成 URL、禁止凭记忆填充、禁止拼接或改写 URL。
   - **链接必须来自检索结果原文**：报告里每个链接必须是本次检索结果中真实出现的 URL；禁止根据域名/ID 格式推测编造、禁止拼接构造（如按账号 ID 格式造主页链接）。检索结果里没有真实链接的内容，不写该条，或换写有真实来源的内容。
   - **社媒主页链接必须有据可依**：贴「进入主页」按钮前，必须能对应到检索结果中真实出现的账号主页链接；检索结果中没有该平台主页链接时，该平台行写「未检索到账号主页」，禁止贴推测的链接。
2. **来源可点且文字可辨**：每个内容点的来源是美观的可点链接，**按钮文字必须描述来源**（如「百科」「公司官网」「新闻」「财报」），禁止统一写「来源」「来源1」「来源2」；禁止裸 URL、禁止纯文字「来源：xxx」。
3. **死链不进报告（渲染前必须自检）**：所有引用链接在渲染前逐条自检可访问性——打不开的（404/567/跳登录墙/403 拒绝）不引用；**登录墙/验证码/403 反爬页必须标注「需登录」或「访问受限」，优先选免登录来源**（官网/新闻/百科）；拿不准能否打开的链接宁可不引用，也不放进报告。
4. **来源不重复**：同一 URL 在报告中最多出现 2 次；**降级场景例外**：同一官网 URL 最多 3 次，且必须至少配 1 个非官网来源（新闻/百科），禁止全篇只用一个来源反复引用；超过上限必须换替代来源，或如实标注「以同一来源为准」。
5. **查不到如实写**：社媒/言论/履历查不到 → 写「未检索到」+ 检索记录（平台×关键词→结果），证明真查过。
6. **不调查不背调**：只研究公开职务身份与公开言论，不深挖隐私、不查涉诉记录、不突破登录墙。
7. **敏感词零**：报告不含真人名/公司名/敏感词（违规词出现即不合格）。
8. **机器校验 + 聊天交付（硬门禁）**：渲染后必须运行 `python scripts/verify-links.py <报告> <目标实体>`，并在聊天中**原样粘贴脚本输出**（从 `[OK]`/`[FAIL]` 起逐行，禁止转述、禁止自称"已通过"）。**只有脚本输出为 `[OK] 全部通过` 才允许交付**，交付句 `📄 报告已生成：<路径>（机器校验通过）`；**脚本输出 [FAIL] 时禁止交付**，必须按 FAIL 清单逐条修正后重跑直至 [OK]（校验范围含链接 / 敏感词 / 来源 / HTML 结构）。浏览器自动弹出只是辅助，不是交付标准。
9. **渲染前逐项完整性自检（交付前必做，缺一不可）**：渲染完成后、交付前，逐项核对以下清单，全部通过才交付：
   - **每处内容点都有来源**：逐条检查所有正文内容（身份/轨迹/双声道/谈资/避雷/破冰/Gap），每条必须带可点来源链接；缺来源的内容点，要么补上来源，要么如实标注「未检索到出处」，禁止裸内容无来源。
   - **社媒行有来源或如实标注**：每个平台行必须有账号主页链接、或「未检索到」、或「仅见报道提及」三选一，禁止空白行。
   - **链接不重复**：数一遍全文每个 URL 出现次数，超上限（普通 2 次/降级场景官网 3 次）的先替换再交付。
   - **死链已排除**：所有链接渲染前已自检可访问性，无 404/567/403/登录墙未标注项。
   - 自检结果在交付句前用一行说明（如「已自检：来源完整 ✓ 链接无重复 ✓ 无死链 ✓」）。

## 报告结构（HTML）

```
🎯 破冰抓手  （身份一句话 + 定位层级 + 1 个破冰抓手）
01 身份与定位 （姓名/公司/职位 + 降级链命中层级，带来源）
02 背景轨迹  （3-4 个节点，带来源）
03 双声道    （💬他/她说 vs 📰外界 + Gap 解读；无公开言论如实空）
04 谈资与避雷（3 个破冰谈资 + 1 避雷，带来源）
05 社交账号  （微博/领英/X/抖音… 账号+粉丝+链接按钮）
附录  已检索平台清单（✓/未检索到 + 理由）· 免责声明
```

## 免责声明

- 社媒区：`社交账号基于公开检索结果整理，受各平台可见性、登录墙与检索覆盖限制，账号无法保证收录完全，仅供会前参考；未检索到 ≠ 不存在。`
- 人物信息区：`本简报仅基于公开职务信息与公开言论整理，不涉及个人隐私调查，仅供会前参考；未检索到 ≠ 该人物无相关信息。`

## 输出

单文件 HTML（report-template.html 填充），保存 `person-brief-<人名>.html`，**聊天中输出交付句**（浏览器弹出仅辅助，不是交付标准）。
